# Front standalone
# Back standalone
# Full stack (orquestación)

## Environment Rules

- Standalone is only used for `feature` and `dev`
- QA and MAIN are only executed via the orchestrator
- No standalone containers should exist for QA or MAIN

# cd ..
# chmod +x utils/launch.sh
